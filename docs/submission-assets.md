# Submission assets

Browser-rendered stills for the project gallery and demo planning:

- [`submission-incident-open.png`](screenshots/submission-incident-open.png) — initial `INC-042` workspace
- [`submission-staged-not-applied.png`](screenshots/submission-staged-not-applied.png) — exact staged change before approval
- [`submission-human-approved.png`](screenshots/submission-human-approved.png) — visible human approval state
- [`submission-resolved.png`](screenshots/submission-resolved.png) — recovered topology, metrics, and timeline
- [`submission-incident-launcher.png`](screenshots/submission-incident-launcher.png) — reusable Incident Pack platform and local import
- [`submission-capability-firewall-approved.png`](screenshots/submission-capability-firewall-approved.png) — pack-specific approval and visible apply availability

These stills are generated by the Playwright submission-asset mode against the production UI, registry, and tool executors. The browser test supplies a test-only `document.modelContext` harness; real supported-browser WebMCP evidence is recorded separately in [judging-evidence.md](judging-evidence.md), including deployed-origin Gate 8 evidence.

Regenerate from a clean checkout:

```bash
CAPTURE_SUBMISSION_ASSETS=1 npm run test:e2e
```

PowerShell:

```powershell
$env:CAPTURE_SUBMISSION_ASSETS = "1"
npm run test:e2e
Remove-Item Env:CAPTURE_SUBMISSION_ASSETS
```

Do not substitute these stills for the required under-three-minute live WebMCP demo video.
